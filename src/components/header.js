function Header() {
  return (
    <header>
      <div className="logo">logo</div>
      <nav className="navbar">
        <a href="#">Home</a>
        <a href="#">About us</a>
        <a href="#">Services</a>
        <a href="#">Blogs</a>
      </nav>
      <button className="yellow-transparent-btn header-btn">GIFT FINDER</button>
    </header>
  );
}

export default Header;
